import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'plan-a-frontend-challenge-gabriel-cordeiro',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
